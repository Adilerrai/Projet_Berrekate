import { LazyDirectiveDirective } from './lazy-directive.directive';

describe('LazyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
